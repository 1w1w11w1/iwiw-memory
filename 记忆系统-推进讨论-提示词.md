# 记忆系统推进讨论 · 上下文提示词

> 用法：在新会话开场把本文档全文粘贴给 AI（或让它读取本文件），即可恢复全部上下文。
> 讨论语言：中文。

---

## 0. 我是谁、我在做什么

我有一个**自研的记忆系统模型**（对话上下文：FuRongJun-1999/dsh-memory，即「灵枢 AEIS / 白箱智能」路线——如果这就是我的项目，下文审计直接适用；不是请先纠正我）。

核心主张：把「智能论 v3.4」协议工程化——条件路由表 → 组合生成 → 自校验 → 知识固化 → LLM 降级为外部校验器；五层记忆（锚点/结构/知识/情境/自我）+ 知识飞轮（验证→归纳→联想→蒸馏→推演）+ 元认知（cognition / self_reliability / emotional_bias / 递归反思）。

**我的困境：不知道下一步该先推进什么。** 当前状态与已知短板（来自一次严肃的源码级竞品审计）：

- 部署重：TS 插件 + Python wheel 双语言；docker/server.py 服务端
- 零量化证据：没有任何标准 benchmark，效果全靠自述
- 适配旧：DSH 0.1.1-rc.2（宿主已 0.1.2+）；config 依赖 profile 层手动 override，丢失即坏
- 工具面爆炸：82 个工具；仓库混杂 POC 脚本、角色扮演 web、哲学文档
- 定位自述「AGI 研究人员向，非消费级」——但这正是用户流失的原因

---

## 1. 竞品审计结论（源码级，2026-09 实测）

六维度：本地部署 / 架构清晰 / 记忆效果 / 长对话找回 / 自省进化。

| 插件 | ⭐ | 一句话机制 | 关键证据 |
|---|---|---|---|
| dsh-meow-memory | 82 | node:sqlite 七层记忆（soul/user/project/fact/lesson/topic/rules）；关键词命中 top-2 注入；空闲 dream 三阶段整理；**压缩后重注入** | 打分=交集×idf×覆盖率×艾宾浩斯衰减×importance；零原生依赖 |
| graph-memory | 593 | **上下文接管**：保留最近 5 轮，旧历史→图节点（TASK/SKILL/EVENT）+向量+FTS5，召回带精确来源 | 20 轮实测第 20 轮 token **−70.58%**，20/20 通过；自省机制弱 |
| StrataGate-AgentMemory | 37 | L0–L5 分层（旧记忆变浅层索引）+ 事件卡带来源 + **证据门**（回答前判断证据是否够）+ 只强化实际使用的记忆 | LoCoMo conv-26 **80.46%** vs Mem0 63.22%，Temporal +40pp；Judge prompt 给了 SHA-256 |
| dsh-layered-memory | 13 | L0 捕获→L1 原子→L2 场景→L3 画像蒸馏；agent/pre-step 每步自动注入；BM25+sqlite-vec 真混合检索 | design/ 七份 spec + ADR + bench 目录 + 蒸馏成本看板 |
| mnemon | 558 | Go 单二进制，LLM-supervised（宿主 LLM 判断、二进制做确定性计算）；四图（temporal/entity/semantic/causal） | 零 API key；可选 Ollama 本地 embedding；hooks 驱动生命周期 |
| dsh-memory-evolve | 286 | 五轨记忆 + 回合内自我审查（advisor）+ 技能自进化 + 情绪反馈记录 | 自省叙事最强但检索弱、全家桶混杂 |

**共性观察**：所有头部插件都是「对话即记忆」——hook 会话事件自动捕获、每步自动召回注入、后台自动整理。没有一家让用户敲指令来记忆或检索。

---

## 2. 核心设计张力：注意力成本（本轮讨论焦点）

我的批评：**市面插件把内部机制泄漏给了用户**。三个泄漏点：

1. **写入闸门外置**——「请记一下」由用户显式发起、写入需用户确认。理由是防记忆污染（错误记忆会在之后所有会话注入，比遗忘更危险），但成本转嫁给了用户注意力。
2. **检索显式化**——「帮我找一下记忆」。自动命中不达标时用户被迫显式发起。
3. **整理指令化**——/dream 之类的手动通道成为实际主路径。

**精确化的解法方向（待讨论）**：

- 闸门位置后移：沉默写入 + **事后异步审查**（审查失败自动撤销/降权），替代事前人工确认。我的白箱理念**天然支持这条路**——全链路留痕、可审计，本来就是「事后可回溯」的架构，事前确认反而是和理念不一致的妥协。
- 读取完全自动化：pre-step 自动注入（layered-memory / graph-memory 已示范），检索质量靠召回排序而非用户复述。
- 整理后台化 + 降级为提示（不是指令）。

---

## 3. 候选推进路径（帮我排序/取舍/补充）

- **A. 证据优先**：先跑 LoCoMo（StrataGate 已示范单对话评测的完整协议与开源框架），用一个可引用的数字替代「AGI 级」叙事。评测会反向暴露管线真实短板。
- **B. 部署收敛**：消除 Python 侧依赖（或 wheel 一键化 + 健康自检）；cordis.patch.yml 改 bundle patch 自动注册（meow-memory / memory-evolve 均已示范，install 后零手动配置）；适配 DSH 0.1.2。
- **C. 注意力成本重构**：按第 2 节方向改造写入/读取/整理三面，把「白箱可审计」从理论卖点变成交互优势（用户从不被确认打断，但随时可回溯每条记忆的来源与判定链）。
- **D. 工具面收敛**：82 工具 → core 档只留认知主链（remember/recall/verify/reflect…），brain 档进阶；工具清单膨胀本身就是上下文污染。
- **E. 定位重构**：「AGI 研究平台」→「可验证的记忆引擎」，理论文档移出主仓库入口，benchmark 与架构文档前置。

---

## 4. 讨论规则

1. 直接、基于证据，不客套；反对「这个想法很棒」式开头。
2. 涉及竞品时引用上面审计事实；需要新证据时先查再断言。
3. 每轮聚焦一个待决问题，给出取舍理由，而不是并列罗列所有选项。
4. 对我的模型：短处直说；「理论自洽」不等于「有效」，有效性必须有量化或可复现实验支撑。
